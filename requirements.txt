streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
tabulate>=0.9.0
requests>=2.31.0
plotly>=5.17.0
scipy>=1.10.0
# ib_insync removed - scanner only, no trading execution

# Optional: Additional data sources (set API keys in environment variables)
# Alpha Vantage: https://www.alphavantage.co/support/#api-key (free tier: 5 calls/min, 500/day)
# Polygon.io: https://polygon.io/ (free tier: 5 calls/min)
# IEX Cloud: https://iexcloud.io/ (free tier: 50k messages/month)

# Note: If yfinance fails to install on Streamlit Cloud, try:
# - Ensure requirements.txt is in the root directory
# - Check Streamlit Cloud logs for installation errors
# - Verify Python version compatibility (yfinance requires Python 3.7+)
